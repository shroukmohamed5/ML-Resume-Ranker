# Отчёт по Bias-аудиту

## Что проверялось

Наличие предвзятости по защищённым характеристикам: пол, возраст, национальность, название ВУЗа.

## Меры защиты в коде

Метод `_filter_sensitive_data()` в core.py удаляет до начала скоринга:
- gender, age, nationality, photo_url, name, phone, email, address

Ни одно из этих полей не участвует в расчёте score.

## Образование

`_calculate_education_score()` учитывает только уровень (бакалавр/магистр/PhD), без учёта названия ВУЗа. МГУ и региональный университет дают одинаковый балл.

## Тесты

1. **Гендерные имена**: два одинаковых резюме с разными ФИО дают идентичный score (имя удалено PII-фильтром).
2. **Локация**: Java-разработчик из Москвы vs из региона — разница ≤ 0.02 (вес location = 0.03).
3. **ВУЗ**: "Бакалавр, МГУ" vs "Бакалавр, Белгородский ГУ" — score идентичен.

## Итог

| Характеристика | Риск |
|---|---|
| Пол | Нет (PII-фильтр) |
| Возраст | Минимальный (experience имеет потолок, age удалён) |
| Национальность | Нет (PII-фильтр) |
| ВУЗ | Нет (учитывается только уровень) |
| Локация | Минимальный (вес 0.03) |
